# Output Template

Use this template when returning a complete Xiaohongshu image-post package.

## Positioning

- `postType`:
- `targetReader`:
- `coreArgument`:
- `whyNow`:

## Title Options

1.
2.
3.

## Cover Options

### Cover A

- `headline`:
- `subline`:

### Cover B

- `headline`:
- `subline`:

## Page Plan

1. `pageRole`:
   `goal`:
2. `pageRole`:
   `goal`:

Continue for the required page count.

## Page Copy

### Page 1

- `pageRole`:
- `headline`:
- `subhead`:
- `body`:
- `emphasisLines`:

### Page 2

- `pageRole`:
- `headline`:
- `subhead`:
- `body`:
- `emphasisLines`:

Continue for all pages.

## Caption

- `captionTitle`:
- `captionBody`:
- `cta`:
- `hashtags`:

## Layout Brief

- `visualDirection`:
- `canvasSize`:
- `pageCount`:
- `cardStyle`:
- `typographyDirection`:
- `accentColorDirection`:
- `specialNotes`:
